/*Write a program to enter base and height of a triangle and find its area */

public class AreaOfTriangle {
	public static void main(String[] args) {
		int base = 10;
		int height = 20;
		System.out.println(
"Area of triangle(" + base + ", " + height + ") " + (base * height / 2));
	}
}